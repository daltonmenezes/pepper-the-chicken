function difficulty() {
  if (chicken.score >= 20)
      speed = 12;

  if (chicken.score >= 40)
      speed = 16;

  if (chicken.score >= 80)
      speed = 19;

  if (chicken.score >= 100)
      speed = 24;
}
